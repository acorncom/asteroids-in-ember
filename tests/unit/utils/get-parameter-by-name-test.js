import getParameterByName from 'asteroids-ember/utils/get-parameter-by-name';
import { module, test } from 'qunit';

module('Unit | Utility | get parameter by name');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getParameterByName();
  assert.ok(result);
});
